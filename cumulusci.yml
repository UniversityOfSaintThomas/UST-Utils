minimum_cumulusci_version: '3.31.0'
project:
    name: UST-Utils
    package:
        name: UST-Utils
        api_version: '55.0'
    git:
        default_branch: 'main'
    source_format: sfdx

tasks:
    deploy:
        options:
            path: force-app